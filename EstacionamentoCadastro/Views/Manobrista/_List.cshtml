@model PagedList.Core.IPagedList<ManobristaViewModel>

@if (Model.Count > 0)
{
    @Html.Hidden("Pagina")
    @Html.Hidden("Ordenacao")
    @Html.Hidden("OrdenacaoDirecao")
    <div class="table-responsive">
        <table class="table table-striped table-bordered TableSearch" id="perfis">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>CPF</th>
                    <th>Data de Nascimento</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>@item.NomeManobrista</td>
                    <td>@item.CPF</td>
                    <td>@item.DataNascimento</td>
                    <td class="col-md-1">
                        <a href="@Url.Action("Edit", "Manobrista", new { id = item.Id })" class="btn btn-sm btn-info"><i class="fa fa-pencil-square-o fa-fw"></i>Editar</a>
                    </td>
                    <td class="col-md-1">
                        <a href="@Url.Action("Excluir", "Manobrista", new { id = item.Id })" class="btn btn-sm btn-danger"><i class="fa fa-pencil-square-o fa-fw"></i>Excluir</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
        <pager list="@Model" />
    </div>
}
else
{
    <div class="col-md-12 text-center">
        Não há Manobristas
    </div>
}
